= form_for @user_project do |f|
  - if @user_project.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_project.errors.count, "error")} prohibited this user_project from being saved:"
      %ul
        - @user_project.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user_id
    = f.number_field :user_id
  .field
    = f.label :project_id
    = f.number_field :project_id
  .actions
    = f.submit 'Save'
